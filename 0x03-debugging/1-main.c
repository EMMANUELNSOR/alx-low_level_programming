#include <stdio.h>
/**
 * main. cause an infinite loop
 */

int main(void)
{
	int i;

	printf("infinite loop incoming :(\n");

	i=0;

	/*
	 * while (i<10)
	 * {
	 * puchar(i)
	 * }
	 */
	print("infinite loop avoided! \\o/\n");
	return(0);
}
